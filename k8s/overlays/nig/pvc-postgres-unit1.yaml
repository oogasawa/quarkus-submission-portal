apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-unit1-pvc
  namespace: submission-portal
  labels:
    app: postgres
    unit: unit1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
