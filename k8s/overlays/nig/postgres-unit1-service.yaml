apiVersion: v1
kind: Service
metadata:
  name: postgres-unit1
  namespace: submission-portal
  labels:
    app: postgres
    unit: unit1
spec:
  selector:
    app: postgres
    unit: unit1
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
